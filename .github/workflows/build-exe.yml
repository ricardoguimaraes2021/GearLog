# This workflow is disabled because setup scripts were removed in commit abca156
# The build_exe.py and setup.py files are no longer part of the repository
# If you need to rebuild these scripts, they can be recreated based on the commit history

# name: Build Windows Executable
#
# on:
#   workflow_dispatch:  # Manual trigger
#   push:
#     tags:
#       - 'v*'  # Trigger on version tags like v1.0.0
#
# jobs:
#   build-windows-exe:
#     runs-on: windows-latest
#     
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4
#       
#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.11'
#       
#       - name: Install build dependencies
#         run: |
#           python -m pip install --upgrade pip
#           pip install -r requirements-build.txt
#       
#       - name: Build executable
#         run: python build_exe.py
#       
#       - name: Upload executable artifact
#         uses: actions/upload-artifact@v4
#         with:
#           name: GearLogSetup.exe
#           path: dist/GearLogSetup.exe
#           retention-days: 30
#       
#       - name: Create Release (if tag)
#         if: startsWith(github.ref, 'refs/tags/')
#         uses: softprops/action-gh-release@v1
#         with:
#           files: dist/GearLogSetup.exe
#           draft: false
#           prerelease: false
#           generate_release_notes: true
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

