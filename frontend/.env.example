# API Configuration
# Base URL for the backend API (without /api/v1 suffix)
# The application will automatically append /api/v1 to this URL
VITE_API_URL=http://localhost:8000/api/v1

# Note: In Vite, all environment variables must be prefixed with VITE_
# to be exposed to the client-side code.
